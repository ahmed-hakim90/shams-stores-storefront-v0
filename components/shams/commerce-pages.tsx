import Link from 'next/link'
import { ArrowRight, Check, Package, ShieldCheck, Truck } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { commerce, formatMoney } from '@/lib/commerce'
import { ProductCard } from './product-card'

export function CartPage() { const products = commerce.products.list().slice(0, 2); return <main className="mobile-storefront-page mx-auto max-w-[1200px] px-4 py-8 sm:px-6 sm:py-12"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand-ink">Your basket</p><h1 className="mt-2 text-3xl font-semibold sm:text-4xl">Shopping cart</h1><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="rounded-(--radius-card) border border-border"><div className="border-b border-border p-5 font-medium">{products.length} items</div>{products.map((product) => <div key={product.id} className="flex gap-4 border-b border-border p-5 last:border-0"><div className="relative size-24 shrink-0 rounded-(--radius-control) bg-white"><img src={product.image} alt={product.name} className="size-full object-contain p-2" /></div><div className="min-w-0 flex-1"><p className="text-xs font-semibold uppercase text-brand-ink">{product.brand}</p><h2 className="mt-1 font-medium">{product.name}</h2><p className="mt-1 text-sm text-muted-foreground">Quantity 1</p></div><p className="font-semibold">{formatMoney(product.price)}</p></div>)}</section><aside className="h-fit rounded-(--radius-card) border border-border bg-card p-5"><h2 className="font-semibold">Order summary</h2><div className="mt-5 flex justify-between text-sm"><span>Subtotal</span><span>{formatMoney({ amount: products.reduce((sum, product) => sum + product.price.amount, 0), currency: 'EGP' })}</span></div><div className="mt-3 flex justify-between text-sm text-muted-foreground"><span>Shipping</span><span>Calculated at checkout</span></div><div className="my-5 border-t border-border pt-5"><div className="flex justify-between text-lg font-semibold"><span>Total</span><span>{formatMoney({ amount: products.reduce((sum, product) => sum + product.price.amount, 0), currency: 'EGP' })}</span></div></div><Link href="/checkout" className="flex min-h-12 items-center justify-center rounded-(--radius-control) bg-brand px-5 text-sm font-medium text-brand-foreground">Checkout</Link><Link href="/" className="mt-4 flex items-center justify-center text-sm text-brand-ink">Continue shopping</Link></aside></div></main> }

export function CheckoutPage() { return <main className="mobile-storefront-page mx-auto max-w-[1200px] px-4 py-8 sm:px-6 sm:py-12"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand-ink">Secure checkout</p><h1 className="mt-2 text-3xl font-semibold sm:text-4xl">Complete your order</h1><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="flex flex-col gap-6"><CheckoutSection title="Contact information"><input aria-label="Email address" type="email" placeholder="Email address" autoComplete="email" className="min-h-12 rounded-(--radius-control) border border-border bg-background px-4 text-base outline-none focus:border-brand" /><input aria-label="Full name" placeholder="Full name" autoComplete="name" className="min-h-12 rounded-(--radius-control) border border-border bg-background px-4 text-base outline-none focus:border-brand" /><input aria-label="Mobile number" type="tel" placeholder="Mobile number" autoComplete="tel" className="min-h-12 rounded-(--radius-control) border border-border bg-background px-4 text-base outline-none focus:border-brand" /></CheckoutSection><CheckoutSection title="Delivery method"><div className="grid gap-3 sm:grid-cols-2"><label className="rounded-(--radius-control) border border-brand bg-brand/5 p-4"><input type="radio" name="delivery" defaultChecked className="mr-2" />Home delivery<p className="mt-2 text-sm text-muted-foreground">Across Egypt</p></label><label className="rounded-(--radius-control) border border-border p-4"><input type="radio" name="delivery" className="mr-2" />Store pickup<p className="mt-2 text-sm text-muted-foreground">Downtown Cairo or Heliopolis</p></label></div></CheckoutSection><CheckoutSection title="Shipping address"><div className="grid gap-3 sm:grid-cols-2"><input aria-label="First name" placeholder="First name" className="min-h-12 rounded-(--radius-control) border border-border bg-background px-4 text-base" /><input aria-label="Last name" placeholder="Last name" className="min-h-12 rounded-(--radius-control) border border-border bg-background px-4 text-base" /><input aria-label="Address" placeholder="Address" className="min-h-12 rounded-(--radius-control) border border-border bg-background px-4 text-base sm:col-span-2" /></div></CheckoutSection><CheckoutSection title="Payment"><div className="rounded-(--radius-control) border border-border p-4 text-sm text-muted-foreground">Payment methods will be available securely at the next step. Card, installments and local methods are supported.</div></CheckoutSection><Button className="min-h-12 w-full sm:w-auto">Place order <ArrowRight data-icon="inline-end" /></Button></section><aside className="h-fit rounded-(--radius-card) border border-border bg-card p-5"><h2 className="font-semibold">Order summary</h2><p className="mt-4 text-sm text-muted-foreground">Your selected products and delivery estimate will appear here.</p><div className="mt-6 flex flex-col gap-3 text-sm text-muted-foreground"><p className="flex gap-2"><ShieldCheck className="size-5 text-brand-ink" />Secure payment</p><p className="flex gap-2"><Truck className="size-5 text-brand-ink" />Track your delivery</p></div></aside></div></main> }

function CheckoutSection({ title, children }: { title: string; children: React.ReactNode }) { return <section className="border-b border-border pb-6"><h2 className="mb-4 text-lg font-semibold">{title}</h2><div className="flex flex-col gap-3">{children}</div></section> }

export function AccountPage() { return <main className="mobile-storefront-page mx-auto max-w-[1200px] px-4 py-8 sm:px-6 sm:py-12"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand-ink">My Shams</p><h1 className="mt-2 text-3xl font-semibold">Account</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Orders', 'Track purchases and deliveries', '/account/orders'], ['Wishlist', 'Saved products and price changes', '/wishlist'], ['Addresses', 'Manage delivery addresses', '/account/addresses'], ['Profile', 'Your contact details', '/account/profile']].map(([title, copy, href]) => <Link key={href} href={href} className="rounded-(--radius-card) border border-border p-5 transition-colors hover:border-brand"><Package className="size-5 text-brand-ink" /><h2 className="mt-5 font-semibold">{title}</h2><p className="mt-2 text-sm text-muted-foreground">{copy}</p><ArrowRight className="mt-5 size-4 text-brand-ink" /></Link>)}</div></main> }

export function OrderPage() { return <main className="mobile-storefront-page mx-auto max-w-[900px] px-4 py-8 sm:px-6 sm:py-12"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand-ink">Order tracking</p><h1 className="mt-2 text-3xl font-semibold">Your orders</h1><div className="mt-8 rounded-(--radius-card) border border-border p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm text-muted-foreground">Order #SH-10482</p><h2 className="mt-1 text-lg font-semibold">Processing</h2></div><span className="rounded-full bg-brand/10 px-3 py-1 text-sm text-brand-ink">Confirmed</span></div><div className="mt-8 grid grid-cols-4 gap-2 text-center text-xs text-muted-foreground"><div><span className="mx-auto mb-2 block size-3 rounded-full bg-brand" />Confirmed</div><div><span className="mx-auto mb-2 block size-3 rounded-full bg-muted" />Preparing</div><div><span className="mx-auto mb-2 block size-3 rounded-full bg-muted" />Shipped</div><div><span className="mx-auto mb-2 block size-3 rounded-full bg-muted" />Delivered</div></div><div className="mt-8 flex items-center gap-3 border-t border-border pt-5 text-sm text-muted-foreground"><Check className="size-5 text-brand-ink" />We’ll email you when your order moves to the next stage.</div></div></main> }

export function WishlistPage() { return <main className="mobile-storefront-page mx-auto max-w-[1440px] px-4 py-8 sm:px-6 sm:py-12"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand-ink">Saved for later</p><h1 className="mt-2 text-3xl font-semibold sm:text-4xl">Wishlist</h1><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{commerce.products.list().slice(0, 4).map((product) => <ProductCard key={product.id} product={product} />)}</div></main> }
